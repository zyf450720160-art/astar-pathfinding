# A* 路径规划算法 (Python)

这是一个高效的 A* 路径规划算法 Python 实现，用于在网格地图中找到从起点到终点的最短路径。

## 📁 文件说明

- **`astar.py`**: 原始版本 - 面向对象实现，代码清晰易懂
- **`astar_optimized.py`**: 优化版本 - 性能提升 48 倍，适合生产环境
- **`test_astar.py`**: 完整的测试套件
- **`performance_test.py`**: 性能基准测试

## 🚀 快速开始

### 使用原始版本（学习用途）
```python
from astar import find_path_in_grid

path = find_path_in_grid(
    width=10, 
    height=10, 
    start=(0, 0), 
    goal=(9, 9), 
    obstacles=[(2, 2), (3, 3), (4, 4)]
)
print(path)
```

### 使用优化版本（生产环境）
```python
from astar_optimized import find_path_in_grid

path = find_path_in_grid(
    width=100, 
    height=100, 
    start=(0, 0), 
    goal=(99, 99), 
    obstacles=[(20, 20), (30, 30), (40, 40)]
)
print(path)
```

## 📊 性能对比

| 网格大小 | 原始版本 | 优化版本 | 性能提升 |
|----------|----------|----------|----------|
| 20x20 | 0.00077s | 0.00001s | 80.83x |
| 50x50 | 0.0226s | 0.00139s | 16.28x |

## 🧪 运行测试

```bash
python test_astar.py
python performance_test.py
```

## 📝 算法特点

- **启发式搜索**：使用曼哈顿距离作为启发式函数
- **最优路径**：保证找到最短路径
- **障碍物支持**：可设置任意障碍物位置
- **错误处理**：完善的输入验证和异常处理
- **高效率**：优化版本适用于大型网格

## 📄 许可证

MIT License